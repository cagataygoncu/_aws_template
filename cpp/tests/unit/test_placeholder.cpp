// Placeholder unit test. Replace with project-specific tests.
// Wire into CMakeLists.txt with add_test(...) under enable_testing().
#include <cassert>

int main() {
    assert(1 + 1 == 2);
    return 0;
}
